name: "Iterator example"
supportedstandards: []
events:
  - name: found storage values
    parameters:
      - name: values
        type: InteropInterface
  - name: found storage keys
    parameters:
      - name: keys
        type: Any
