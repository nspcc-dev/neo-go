name: "Awesome NEO Token"
supportedstandards: ["NEP-17"]
events:
  - name: Transfer
    parameters:
      - name: from
        type: ByteString
      - name: to
        type: ByteString
      - name: amount
        type: Integer
